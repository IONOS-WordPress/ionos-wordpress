this folder contains snippet catalogs

Snippet catalogs are a collection of PHP code snippets. 

These snippets will be loaded into the plugin ui and can be executed in the WordPress context.

## validate json against json schema

`npx ajv-cli validate -s ./schema.json -d ./default.json`