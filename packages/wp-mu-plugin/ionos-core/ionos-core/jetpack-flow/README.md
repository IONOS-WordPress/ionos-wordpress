# Jetpack flow

This feature implements injection of the jetpack coupon from the hosting environment into a wordpress instance.

# Development

- the jetpack flow redemption can be called by using `http://localhost:8888/wp-admin/admin.php?page=ionos-assistant&setup_action=partner&usecase=jetpack-backup&coupon=xxx`
