# About

stretch-extra acts as a wp-env shim to include the php code targeting stretch mouted code from /opt/WordPress/extra/index.php

This must-use plugin provisions additional WordPress code into a SFS stretch hosted WoirdPress account.

# Development

show logs : `pnpm run wp-env run wordpress tail -f /var/www/html/wp-content/debug.log`
